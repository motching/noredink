package object noRedInk{

  def findMax(array:Array[Int], prevMax:Int):Int = {
    if (array.length = 0) {
      return prevMax
    }

    val curMax =
      if (array.head > prevMax) {
        array.head
      } else {
        prevMax
      }

    findMax(ar)

    }

  def main(args: Array[String]) :Unit={
    println("max: " + findMax(Array(1, 4, 5, 2, 3), 0))
  }
}
