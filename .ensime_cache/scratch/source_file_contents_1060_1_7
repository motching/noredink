package object noRedInk{

  def atoi(string:String, accu:Int):Int = {
    if (string.length != 0) {
      pr
      atoi(string.tail, accu*10 + string.head.toInt)
    }
    accu
    }

  def main(args: Array[String]) :Unit={
    println(atoi("1986", 0))
  }
}
